import { ViewErrorDirective } from './view-error.directive';

describe('ViewErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewErrorDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
