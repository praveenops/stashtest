import { ViewInvalidDirective } from './view-invalid.directive';

describe('ViewInvalidDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewInvalidDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
