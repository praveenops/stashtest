import { ViewResolvedDirective } from './view-resolved.directive';

describe('ViewResolvedDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewResolvedDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
