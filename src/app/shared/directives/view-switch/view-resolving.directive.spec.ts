import { ViewResolvingDirective } from './view-resolving.directive';

describe('ViewResolvingDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewResolvingDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
